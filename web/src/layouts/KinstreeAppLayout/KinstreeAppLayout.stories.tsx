import type { Meta, StoryObj } from '@storybook/react'

import KinstreeAppLayout from './KinstreeAppLayout'

const meta: Meta<typeof KinstreeAppLayout> = {
  component: KinstreeAppLayout,
}

export default meta

type Story = StoryObj<typeof KinstreeAppLayout>

export const Primary: Story = {}
